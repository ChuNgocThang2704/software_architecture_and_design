spring:
  application:
    name: tour-service
  datasource:
    url: jdbc:mysql://localhost:3306/tour_db?createDatabaseIfNotExist=true
    username: root
    password: pbhyclls
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8084
