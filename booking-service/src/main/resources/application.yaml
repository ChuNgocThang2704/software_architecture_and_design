spring:
  application:
    name: booking-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/booking_db?createDatabaseIfNotExist=true}
    username: root
    password: pbhyclls
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8083

services:
  user:
    base-url: ${SERVICES_USER_BASE_URL:http://localhost:8081}
  customer:
    base-url: ${SERVICES_CUSTOMER_BASE_URL:http://localhost:8082}
  tour:
    base-url: ${SERVICES_TOUR_BASE_URL:http://localhost:8084}
