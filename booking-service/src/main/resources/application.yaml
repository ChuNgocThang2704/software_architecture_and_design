spring:
  application:
    name: booking-service
  datasource:
    url: jdbc:mysql://localhost:3306/booking_db?createDatabaseIfNotExist=true
    username: root
    password: pbhyclls
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8083

services:
  user:
    base-url: http://user-service:8081
  customer:
    base-url: http://customer-service:8082
  tour:
    base-url: http://tour-service:8084
